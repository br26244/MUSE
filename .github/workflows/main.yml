name: KeepUp
on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch: 

jobs:
  ping-job:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -s "https://muse-jw19.onrender.com"
